# Components

Nested blocks of logic that are fed into a view.

* Size is goverened by the parent
* Always returns a UIElement
* Will either be a selection of element children or a render method call
